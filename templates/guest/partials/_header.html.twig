<!-- SECTION 1 : HERO -->
<header class="hero">
  <div class="navbar" id="mainNavbar">
    <div class="logo">
      <img src="{{ asset('assets/img/logo-arbre.png') }}" alt="Logo Arbre du Voyageur">
       <a href="{{ path('home') }}" class="site-title"><span>WhereIsFlorie</span></a>
    </div>

    <div class="menu">
      <a href="{{ path('home') }}">Accueil</a>
      <a href="#">Mes voyages</a>
      <a href="{{ path('apropos') }}">Qui suis-je</a>
      <a href="{{ path('alltravels') }}">Les experiences utilsateurs</a>
      <a href="{{ path('register') }}">Inscription</a>
      <a href="{{ path('login') }}">Connexion</a>
     
    </div>
  </div>
</header>
